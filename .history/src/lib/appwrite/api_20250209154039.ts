import { INewUser } from "@/types";

export async function createUSerAccount(user: INewUser) {
    try {
        const newAccount = awa
    } catch (error) {
        console.log(error);
        return error;
    }
}